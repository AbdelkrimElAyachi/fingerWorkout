pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "hello jenkins"
            }
        }

        stage('Test') {
            steps {
                echo "test jenkins"
            }
        }

        stage('Deploy') {
            steps {
                echo "deploy jenkins"
            }
        }
    }
}
